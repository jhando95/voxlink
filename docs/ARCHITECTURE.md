# Voxlink Architecture

## Overview

Voxlink is a voice-first desktop application built on a Rust workspace with WebSocket/UDP signaling and Opus audio codec.

## Deployment

- [Setup (non-TLS)](../deploy/setup-server.sh) — one-shot server install.
- [TLS setup](TLS_SETUP.md) — Let's Encrypt for public deployments.
