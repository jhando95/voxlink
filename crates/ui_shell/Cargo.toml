[package]
name = "ui_shell"
version = "0.10.4"
edition = "2021"
build = "build.rs"

[dependencies]
slint = { workspace = true }
shared_types = { path = "../shared_types" }
log = { workspace = true }

[build-dependencies]
slint-build = "1.9"

[dev-dependencies]
png = "0.17"
